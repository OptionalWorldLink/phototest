<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="{{ path('home') }}">Now Digital</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item {% if currentMenu is same as('admin.pictures') %}active{% endif %}">
                    <a class="nav-link" href="{{ path('admin.pictures.index') }}">Photos</a>
                </li>
                <li class="nav-item {% if currentMenu is same as('admin.branches') %}active{% endif %}">
                    <a class="nav-link" href="{{ path('admin.branches.index') }}">Branches</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
